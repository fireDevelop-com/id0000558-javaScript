npm start // by default you can use for start you app:
npm run start-server // for custom names you can use: npm run start-server

npm install nodemon --save  // install in production site
npm install nodemon --save--dev // install in dev site
npm install nodemon -g  // install globally
npm install // update your npm version


{
    "name": "nodejs",
    "version": "1.0.0",
    "description": "complete nodejs",
    "main": "app.js",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "start": "node app.js",
      "start-server":"node app.js"
    },
    "author": "Max",
    "license": "ISC"
  }


  